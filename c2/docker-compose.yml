# docker-compose run app
version: "3"
services:
    app:
        image: python:3.8
        tty: true
        volumes:
            - ./:/work:cached
        working_dir: /work
        environment:
            - HOME=/work
        command: /bin/bash
